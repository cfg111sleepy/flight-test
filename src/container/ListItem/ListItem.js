import React from 'react'

const ListItem = (props) => {

    const { list } = props

    return (
        <div>
            aaaa
            {console.log(list)}
        </div>
    )
}

export default ListItem